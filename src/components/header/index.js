// Import Ultils
import { Container } from '../../utils/class';

// Import Style
import {
    Section, Title
} from './styled';

const Header = () => (
    <Section id="header">
        <Container>
            <Title>Encontre seu criador de conteúdo.</Title>
        </Container>
    </Section>
);

export default Header;